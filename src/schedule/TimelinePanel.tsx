import React from "react";
import { Box } from "@mui/material";

export default function TimelinePanel() {
  return (
    <Box
      sx={{
        height: '100%',
        minHeight: 0,
        overflow: 'auto',
      }}
    />
  );
}
