This folder contains text-field wrappers around the shared UI field components.

- `TestSingleSelectField` — thin wrapper around `SingleSelectField` for trialing changes.
- `TestFreeTypeSelectField` — thin wrapper around `FreeTypeSelectField` for trialing changes.

Use imports like `import { TestSingleSelectField } from '@/shared-ui/text-fields'`.
